# BD com dados pessoais (NIF, IBAN, salários)
rh_manager.sqlite*
*.db
*.db-journal

backups/
!backups/.gitkeep

# Documentos carregados pelos colaboradores
uploads/
!uploads/.gitkeep

# Excel real
EXCEL/
*.xlsm
*.xlsx
!exemplos/*.xlsx

# Credenciais SMTP, super-admins, default_users
config.php

# IDE / OS
.vscode/
.idea/
.claude/
*.swp
*.swo
Thumbs.db
desktop.ini
.DS_Store

# Logs e temporários
*.log
error_log
php_errorlog
*.tmp
*.bak
*.orig

# Notas locais
*.local.md

# Documentação interna (deploy, onboarding) — fica só na pasta
docs/DEPLOY_IT.html
docs/DEPLOY_IT.md
docs/DEV_ONBOARDING.md

# Scripts de importação one-off (referenciam dados reais dos colaboradores)
scripts/

# Base de conhecimento da IA (manuais internos, guiões, regras
# de cumprimento, dados internos). Não vai para o repo.
ai-knowledge/
Guiao_*.pdf
Guião_*.pdf
*.docx
*.doc
